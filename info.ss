#lang setup/infotab
(define name "Semi-persistent Matrices")
(define release-notes
  (list))
(define repositories
  (list "4.x"))
(define blurb
  (list "'Functional' matrices that allow persistent reads but not persistent writes"))
(define scribblings '(("matrix.scrbl" ())))
(define primary-file "main.ss")
(define categories '(datastructures))